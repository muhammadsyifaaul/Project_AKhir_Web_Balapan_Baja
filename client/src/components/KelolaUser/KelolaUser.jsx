export default function KelolaUser({no,username,nama,role}) {
    return (
        <div>
            <h1>{no}</h1>
            <h1>{username}</h1>
            <h1>{nama}</h1>
            <h1>{role}</h1>
        </div>
    )
}